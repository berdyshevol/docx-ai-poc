services:
  - type: web
    name: docx-ai-poc
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    region: oregon
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false   # set manually in the Render dashboard; not checked into git
